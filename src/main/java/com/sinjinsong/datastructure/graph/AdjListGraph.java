package com.sinjinsong.datastructure.graph;

/**
 * 邻接表表示的图
 * 
 * @author New Song
 *
 *//*
enum GraphKind {
	DG, DN, UDG, UDN
}

class InfoType {
	String info;
	// 该弧相关信息的类
}

// 表示弧或边结点类型
class ArcNode {
	int adjVex;// 该弧所指向的顶点的位置（索引）
	ArcNode nextArc;// 指向下一条弧的指针
	InfoType info;// 相关信息
}

class VertexNode<E> {
	E data;// 数据域
	ArcNode firstArc;// 指向第一条依附于该顶点的弧的指针
}

public class AdjListGraph<E> {
	public static final int MAX_VERTEX_NUM = 20;
	VertexNode<E> [] vertexs;//各个单链表构成的数组
	int vexNum,arcNum;
	GraphKind kind;
}
*/